import { useGame } from './state';
export function ConVarConsole(){const {state,patch}=useGame();return <aside className="console"><h2>Mock game bridge</h2><label>Health <input type="range" min="0" max="100" value={state.health} onChange={e=>patch({health:+e.target.value})}/><output>{state.health}</output></label><label>Round time <input type="range" min="0" max="180" value={state.roundTime} onChange={e=>patch({roundTime:+e.target.value})}/><output>{state.roundTime}s</output></label><button onClick={()=>patch({alert:`Alert ${Date.now()%1000}`})}>Fire alert</button><button onClick={()=>patch({alert:''})}>Clear alert</button></aside>}
